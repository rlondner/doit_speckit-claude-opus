# Database connection
DATABASE_URL=postgresql://user:password@localhost:5432/doit

# Demo mode — set to "true" to enable client-side localStorage demo (no database required)
NEXT_PUBLIC_DEMO_MODE=true
